[project]
name = "hexlet-code"
version = "0.1.0"
description = "Flight booking API service"
readme = "README.md"
authors = [
    { name = "Dmitry", email = "xequrt@yandex.ru" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.5",
    "python-dotenv>=1.2.3",
    "uvicorn>=0.52.4",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]

